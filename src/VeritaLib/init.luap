
--- *Unlocking the power of LuaSTG.*
--
--- VeritaLib is a framework meant for use in LuaSTG Sub.
local vlib = {
    Core = require(".Core"),
    Graphics = require(".Graphics"),
    Resources = require(".Resources"),
    -- Menu = require(".Menu"),
}

--- Runtime alias for target resolution
vlib.screen = { width = $(config.target_res.width), height = $(config.target_res.height) }

return vlib
